# Create Virtual Encironment
`python -m venv .venv`

# Activate Virtual Environment
`.\.venv\Scripts\activate`

# Install Requirements
Ensure virtual environment is active. See previous step.
`pip install -r requirements.txt`

